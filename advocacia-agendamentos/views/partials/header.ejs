<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Advocacia - Sistema de Agendamentos</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <h1>Advocacia - Sistema de Agendamentos</h1>
        <% if (Array.isArray(menu) && menu.length > 0) { %>
        <nav>
            <ul>
                <% menu.forEach(function(item) { %>
                    <li><a href="<%= item.href %>"><%= item.label %></a></li>
                <% }) %>
            </ul>
        </nav>
        <% } %>
        <% if (showBack) { %>
        <a href="/" class="btn btn-outline-primary" style="margin: 20px 0 0 20px;">
            <i class="bi bi-arrow-left"></i> Voltar para a página inicial
        </a>
        <% } %>
    </header>