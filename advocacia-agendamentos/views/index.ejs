<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Página Inicial - Sistema de Agendamentos</title>
    <link rel="stylesheet" href="/css/style.css">
    <!-- Bootstrap Icons CDN -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        body {
            background: #f4f4f4;
        }
        .home-icons {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 60px;
            height: 90vh;
        }
        .icon-card {
            display: flex;
            flex-direction: column;
            align-items: center;
            background: #fff;
            border-radius: 16px;
            box-shadow: 0 4px 16px rgba(0,0,0,0.08);
            padding: 40px 50px;
            text-decoration: none;
            color: #222;
            transition: box-shadow 0.2s, color 0.2s;
        }
        .icon-card:hover {
            box-shadow: 0 8px 32px rgba(52, 73, 94, 0.15);
            color: #0d6efd;
        }
        .icon-card i {
            font-size: 4rem;
            margin-bottom: 20px;
        }
        .icon-card span {
            font-size: 1.3rem;
            font-weight: 500;
        }
    </style>
</head>
<body>
    <%- include('partials/header', { menu: [], showBack: false }) %>
    <main>
        <div class="home-icons">
            <a href="/clientes/cadastro" class="icon-card">
                <i class="bi bi-person-plus"></i>
                <span>Cadastro de Clientes</span>
            </a>
            <a href="/agendamentos" class="icon-card">
                <i class="bi bi-calendar-plus"></i>
                <span>Agendamentos</span>
            </a>
        </div>
    </main>
    <%- include('partials/footer') %>
</body>
</html>