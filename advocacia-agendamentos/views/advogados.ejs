<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Advogados Cadastrados</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('partials/header', { 
        menu: [
            { href: '/advogados/cadastro', label: 'Novo Advogado' },
            { href: '/agendamentos', label: 'Agendamentos' }
        ],
        showBack: true
    }) %>
    <h1>Lista de Advogados</h1>
    <table>
        <thead>
            <tr>
                <th>Nome</th>
                <th>E-mail</th>
                <th>Telefone</th>
                <th>Especialidade</th>
                <th>Disponibilidade</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            <% advogados.forEach(advogado => { %>
                <tr>
                    <td><%= advogado.nome %></td>
                    <td><%= advogado.email %></td>
                    <td><%= advogado.telefone %></td>
                    <td><%= advogado.especialidade %></td>
                    <td><%= advogado.disponibilidade || 'Não especificada' %></td>
                    <td>
                        <a href="/advogados/editar/<%= advogado.id %>">Editar</a>
                        <a href="/advogados/excluir/<%= advogado.id %>">Excluir</a>
                    </td>
                </tr>
            <% }); %>
        </tbody>
    </table>
    <a href="/advogados/cadastro">Cadastrar Novo Advogado</a>
    <%- include('partials/footer') %>
</body>
</html>